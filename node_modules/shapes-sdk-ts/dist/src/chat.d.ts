import { createShapesClient } from "./client";
type Message = {
    role: "user";
    content: string;
} | {
    role: "user";
    content: {
        type: "text";
        text: string;
    } | {
        type: "audio";
        audio: string;
    };
};
export declare function createSendMessage(client: ReturnType<typeof createShapesClient>, defaultModel?: string): {
    (message: Message["content"], model?: string): Promise<string | null>;
    model: {
        (message: Message["content"], model?: string): Promise<string>;
        model: {
            [key: string]: (message: Message["content"]) => Promise<string>;
        };
    };
};
export {};
