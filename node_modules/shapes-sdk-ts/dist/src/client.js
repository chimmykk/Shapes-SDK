"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createShapesClient = createShapesClient;
const openai_1 = require("openai");
function createShapesClient({ apiKey, appId, authToken, baseUrl = "https://api.shapes.inc/v1", }) {
    const headers = {};
    if (appId && authToken) {
        headers["X-App-ID"] = appId;
        headers["X-User-Auth"] = authToken;
    }
    return new openai_1.OpenAI({
        apiKey: apiKey !== null && apiKey !== void 0 ? apiKey : "not-needed", // fallback for cases without apiKey
        baseURL: baseUrl,
        defaultHeaders: headers,
    });
}
