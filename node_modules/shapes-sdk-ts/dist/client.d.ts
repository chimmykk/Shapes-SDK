import { OpenAI } from "openai";
export interface ShapesClientOptions {
    apiKey?: string;
    appId?: string;
    authToken?: string;
    baseUrl?: string;
}
export declare function createShapesClient({ apiKey, appId, authToken, baseUrl, }: ShapesClientOptions): OpenAI;
